# Tests

Future test layout:

- `unit/` for isolated contract and service tests.
- `integration/` for sensor-to-engine and database adapter tests.
- `fixtures/` for synthetic telemetry samples.

Detection-specific fixtures should be added only when detection providers are implemented.
